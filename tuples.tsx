// An array with some restrictions on it

const user: (string | number)[] = ['hc', 1];

// Can be only added in specific format + order
const tuser: [string, number, boolean] = ['hc', 1, true];

// real world use case
let rgb: [number, number, number] = [255, 123, 121];

export { }